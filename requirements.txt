# ─── gRPC / Proto ─────────────────────────────────────────────────────────────
grpcio==1.65.1
grpcio-tools==1.65.1
protobuf==5.27.2

# ─── HTTP / Async ─────────────────────────────────────────────────────────────
aiohttp==3.9.5
asyncio==3.4.3

# ─── Data validation ──────────────────────────────────────────────────────────
pydantic==2.8.2

# ─── Gateway ──────────────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
websockets==12.0

# ─── AI / RAG ─────────────────────────────────────────────────────────────────
# google-generativeai 0.7.2 pinned google-ai-generativelanguage 0.6.6, which caps
# protobuf <5.0 — irreconcilable with the protobuf==5.27.2 the generated gRPC stubs
# and grpcio-tools require. 0.8.6 pulls generativelanguage 0.6.15 (protobuf <6), so
# the whole graph resolves. `pinecone-client` is the deprecated package name (recent
# releases are an import-time stub that errors); the current SDK is `pinecone`, which
# still exposes `from pinecone import Pinecone, ServerlessSpec` as rag/ uses it.
google-generativeai==0.8.6
pinecone==9.0.1

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.1
pytest-asyncio==0.23.7
pytest-mock==3.14.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
